# envs/.prod.env
# Values commented out below are expected to be supplied by the host
# platform (e.g. Fly.io, Heroku) as real environment variables.
# DNS_CLUSTER_QUERY= # set by the host

HTTP_INTERFACE=0:0:0:0:0:0:0:0
# PORT= # set by the host
# SECRET_KEY_BASE= # set by the host
PHX_SERVER=true
