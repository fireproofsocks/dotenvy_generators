# envs/.test.env
DNS_CLUSTER_QUERY=

HTTP_INTERFACE=127.0.0.1
# We use a different port during tests so it doesn't clash with dev
PORT=4002
SECRET_KEY_BASE="<%= @secret_key_base_test %>"
PHX_SERVER=false
